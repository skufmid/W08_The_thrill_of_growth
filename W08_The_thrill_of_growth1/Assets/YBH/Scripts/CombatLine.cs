using UnityEngine;

public class CombatLine : MonoBehaviour
{
    public enum linePosition      //전투 위치
    {
        None,
        Taunt,
        Front,
        Middle,
        Back,

    }
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
